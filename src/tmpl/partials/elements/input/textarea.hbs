---
label: 'Textarea Label'
---
<label class="partial" data-partial="elements/input/textarea">
  {{#if label}}
    <span class="label">{{{label}}}</span>
  {{/if}}
  <textarea{{#if name}} name="{{{name}}}"{{/if}}></textarea>
</label>
